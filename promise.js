const loadInfo = (url) => {
    return new Promise((resolve, reject) = {
        if (resolve) {
            const pages = (url);
            return pages;
        } else
    })
};

loadInfo("https://jsonplaceholder.typicode.com/users?postId=1")
.then(() => {

})